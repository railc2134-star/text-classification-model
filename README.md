# Text Classification Model

A model that classifies text into categories using machine learning. It utilizes the PyTorch library and requests library to fetch data from an API. The model translates text into numerical features and trains on the dataset.

Technologies used: Python, PyTorch, pandas, requests

To run the model, simply execute the fil.py script. Ensure you have the necessary libraries installed and the API is running.